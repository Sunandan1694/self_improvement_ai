<h1>Welcome to the Self Improvement AI</h1>
<p>This is your custom homepage 🎉</p>

<div className="min-h-screen bg-gradient-to-br from-gray-900 via-gray-800 to-black text-white">
  {/* Header */}
  <header className="flex justify-between items-center px-8 py-4">
    <div className="flex items-center space-x-2">
      <div className="w-8 h-8 bg-gray-700 rounded flex items-center justify-center font-bold text-xl">AI</div>
      <span className="font-bold text-lg">Self Improvement AI</span>
    </div>
    <nav className="flex space-x-6">
      <a href="#" className="hover:underline">Features</a>
      <a href="#" className="hover:underline">Resources</a>
      <a href="#" className="hover:underline">Community</a>
      <a href="#" className="hover:underline">Pricing</a>
    </nav>
    <div className="flex space-x-2">
      <button className="px-4 py-2 rounded bg-gray-700">Sign In</button>
      <button className="px-4 py-2 rounded bg-cyan-400 text-black font-bold">Get Started</button>
    </div>
  </header>

  {/* Hero Section */}
  <section className="flex flex-col md:flex-row items-center justify-between px-8 py-16 relative">
    <div className="max-w-xl z-10">
      <h1 className="text-5xl font-extrabold mb-4">Unlock Your Best Self</h1>
      <p className="text-xl mb-8">Your AI-powered companion for personal growth, habit tracking, and self-improvement.</p>
      <button className="px-8 py-3 rounded bg-cyan-400 text-black font-bold text-lg shadow-lg">Try it Free</button>
    </div>
    {/* Futuristic background effect */}
    <div className="absolute right-0 top-0 w-2/3 h-full pointer-events-none z-0">
      <div className="w-full h-full bg-gradient-to-tl from-cyan-400/30 via-transparent to-black blur-2xl"></div>
    </div>
    {/* Placeholder for hero image */}
    <div className="w-full md:w-1/2 flex justify-center z-10 mt-8 md:mt-0">
      <div className="w-80 h-56 bg-gray-700 flex items-center justify-center relative">
        <div className="absolute inset-0 flex items-center justify-center">
          <svg width="100%" height="100%">
            <line x1="0" y1="0" x2="100%" y2="100%" stroke="#aaa" strokeWidth="2"/>
            <line x1="100%" y1="0" x2="0" y2="100%" stroke="#aaa" strokeWidth="2"/>
          </svg>
        </div>
      </div>
    </div>
  </section>

  {/* Main Content Preview */}
  <section className="flex justify-center py-12">
    <div className="bg-gray-800 rounded-2xl shadow-2xl flex w-[900px] h-[400px] overflow-hidden">
      {/* Sidebar */}
      <div className="w-48 bg-gray-900 p-4 flex flex-col space-y-4">
        <div className="w-full h-10 bg-gray-700 flex items-center justify-center rounded">
          <span className="text-xs">Sidebar</span>
        </div>
        {/* Placeholder for nav items */}
        <div className="flex-1 flex flex-col space-y-2">
          {[...Array(5)].map((_, i) => (
            <div key={i} className="w-full h-8 bg-gray-700 rounded"></div>
          ))}
        </div>
      </div>
      {/* Main Area */}
      <div className="flex-1 flex">
        {/* Kanban/Goals Board Placeholder */}
        <div className="flex-1 p-6">
          <div className="w-full h-full bg-gray-700 rounded flex items-center justify-center relative">
            <div className="absolute inset-0 flex items-center justify-center">
              <svg width="100%" height="100%">
                <line x1="0" y1="0" x2="100%" y2="100%" stroke="#aaa" strokeWidth="2"/>
                <line x1="100%" y1="0" x2="0" y2="100%" stroke="#aaa" strokeWidth="2"/>
              </svg>
            </div>
            <span className="text-lg text-gray-400 absolute bottom-4 left-4">Goals / Habits Board</span>
          </div>
        </div>
        {/* Activity Feed Placeholder */}
        <div className="w-72 bg-gray-900 p-4 flex flex-col">
          <div className="w-full h-10 bg-gray-700 flex items-center justify-center rounded mb-4">
            <span className="text-xs">Activity Feed</span>
          </div>
          <div className="flex-1 flex flex-col space-y-2">
            {[...Array(5)].map((_, i) => (
              <div key={i} className="w-full h-8 bg-gray-700 rounded"></div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>
</div>